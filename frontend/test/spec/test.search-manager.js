var Espo = Espo || {};


describe("SearchManager", function () {
	var searchManager;
	
	beforeEach(function () {
		var collection = {
			name: 'some',
		}		
		searchManager = new Espo.SearchManager(collection, 'list', null);		
	});
		
	


});
